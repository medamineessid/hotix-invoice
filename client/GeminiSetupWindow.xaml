<Window x:Class="Hotix.InvoiceClient.GeminiSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration Gemini" Height="320" Width="480"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Topmost="True">

    <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="#3E3E3E" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Configuration optionnelle — Gemini" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10" />

            <!-- Body -->
            <StackPanel Grid.Row="1">
                <TextBlock Foreground="#F0F0F0" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,20">
                    Pour une meilleure précision d'extraction, vous pouvez configurer une clé API Gemini gratuite. Sans clé, l'extraction locale (OCR) reste entièrement disponible. Vous pouvez configurer cette clé plus tard depuis les paramètres.
                </TextBlock>

                <TextBlock Text="Clé API Gemini" Foreground="#A0A0A0" Margin="0,0,0,6" />
                <PasswordBox x:Name="GeminiKeyBox" Height="34" Background="#2D2D2D" Foreground="White" 
                             BorderBrush="#3E3E3E" Tag="Collez votre clé ici..." 
                             VerticalContentAlignment="Center" Padding="8,0" />
                
                <TextBlock x:Name="MessageLabel" FontSize="12" Margin="0,8,0,0" />
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Obtenir une clé gratuite" Style="{Binding Source={StaticResource GrayButtonStyle}}" 
                        Width="160" Margin="0,0,10,0" Click="GetApiKey_Click" />
                <Button Content="Ignorer" Style="{StaticResource GrayButtonStyle}" 
                        Width="80" Margin="0,0,10,0" Click="Ignore_Click" />
                <Button Content="Enregistrer" Style="{StaticResource RedButtonStyle}" 
                        Width="100" Click="Save_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
